// another package, but this one can be consumed from outside
// of this project, so for example, for reusable code, etc..
package foo

// a really simple method, it just returns what you gave it
func Foo(param string) string {
	return param
}
